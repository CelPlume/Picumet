// 管理服务类型
import type { User, Announcement, SystemSettings, AdminStats } from '@shared/types';
import type { UserUpdateRequest, SettingsRequest, AnnouncementRequest } from './schemas';

export type {
  User,
  Announcement,
  SystemSettings,
  AdminStats,
  UserUpdateRequest,
  SettingsRequest,
  AnnouncementRequest,
};
